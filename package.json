{
  "name": "redis.q",
  "version": "0.1.0",
  "description": "a tiny message queue wrapper for redis",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec -t 5000 test/*.js",
    "test-cov": "istanbul cover node_modules/.bin/_mocha -- -R dot -t 5000 test/*.js",
    "test-travis": "istanbul cover node_modules/.bin/_mocha --report lcovonly -- -R dot -t 5000 test/*.js"
  },
  "repository": "onebook/redis.q",
  "keywords": [
    "message",
    "queue",
    "redis"
  ],
  "files": [
    "index.js"
  ],
  "author": "haoxin",
  "license": "MIT",
  "devDependencies": {
    "ioredis": "1",
    "istanbul-harmony": "0",
    "mocha": "2"
  }
}
